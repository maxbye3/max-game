{
  "name": "max-game",
  "version": "1.0.0",
  "private": true,
  "description": "Max's Overworld - a browser overworld game",
  "scripts": {
    "build": "npm run generate:dialogues && npm run generate:battle-avatars && npm run typecheck && npm test && npm run bundle",
    "clean:dist": "node scripts/clean-dist.mjs",
    "bundle": "npm run clean:dist && esbuild js/main.ts js/internal.ts js/niall-fight.ts --bundle --minify --outdir=dist --entry-names=[name]-bundle --format=iife",
    "extend:internal-collision": "node scripts/extend-internal-collision.mjs && npm run generate:internal-collision",
    "generate:internal-collision": "node scripts/generate-internal-collision.mjs",
    "generate:dialogues": "node scripts/generate-dialogues.mjs",
    "generate:battle-avatars": "node scripts/generate-battle-avatars.mjs",
    "optimize:internal-assets": "node scripts/optimize-internal-assets.mjs",
    "watch": "npm run generate:dialogues && npm run clean:dist && esbuild js/main.ts js/internal.ts js/niall-fight.ts --bundle --outdir=dist --entry-names=[name]-bundle --format=iife --watch",
    "test": "node scripts/run-tests.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "typescript": "^5.9.2"
  }
}
